@*@using ShareData.Common;
@using ShareData.Entities.Dto

@model GetStatisticsDto;
*@
<div class="row">
    @* @foreach (var item in Model.CategoriesStatistics)
    {*@
    @* <div class="col-xl-3 col-lg-6 col-md-6 col-12">
    <div class="card gradient-mint">
    <div class="card-content">
    <div class="card-body py-0">
    <div class="media pb-1">
    <div class="media-body white text-left">
    <h3 class="font-large-1 white mb-0">@item.Total</h3>
    <span>@(L("QualifiedAgencies." + item.Category.ToString()))</span>
    </div>
    <div class="media-right white text-right">
    <i class="ft-activity font-large-1"></i>
    </div>
    </div>
    </div>
    <div id="Widget-line-chart" class="height-75 WidgetlineChart WidgetlineChartshadow mb-2">
    </div>
    </div>
    </div>
    </div>*@
    @*}*@
</div>
@*
<div class="row match-height">
    <div class="col-xl-6 col-12">
        <div class="card">
            <div class="card-header">
                <h4 class="card-title">@L("QualifiedAgencies.Statistics.Categories")</h4>
            </div>
            <div class="card-content">
                <div class="card-body">
                    <div class="row">
                        @foreach (var item in Model.CategoriesStatistics)
                        {
                            <div class="col-6 mb-5">
                                <span class="mb-1 text-muted d-block">@Math.Round(item.Percentage, 2)% - @(L("QualifiedAgencies." + item.Category.ToString()))</span>
                                <div class="progress" style="height: 5px;">
                                    <div class="progress-bar bg-info" role="progressbar" style="width: @((int)item.Percentage)%;" aria-valuenow="@((int)item.Percentage)" aria-valuemin="0" aria-valuemax="100"></div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-xl-6 col-lg-12">
        <div class="card">
            <div class="card-content">
                <div class="card-body">
                    <h4 class="card-title mb-4">
                        <span class="">@L("QualifiedAgencies.Statistics.ActivityTypes")</span>
                    </h4>
                    <ul class="list-group mb-3">
                        @foreach (var item in Model.ActivityTypesStatistics)
                        {
                            <li class="list-group-item">
                                <span>@item.ActivityTypeName</span>
                                <span class="badge bg-light-info float-right">@item.Total</span>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
*@